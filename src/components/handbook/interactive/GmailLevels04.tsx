'use client'
import React,{useState}from'react'
import styles from '../handbook.module.css'
const levels=[{v:'1',name:'Soạn draft email',badge:'✓ NÊN BẮT ĐẦU',kind:'safe',desc:'Hermes chỉ soạn bản nháp — chưa có gì ra ngoài. Bạn đọc, sửa rồi tự gửi.',rec:'Mức người mới nên dùng nhiều nhất.'},{v:'2',name:'Kiểm tra / sửa email CEO viết',badge:'✓ RẤT PHÙ HỢP',kind:'safe',desc:'Bạn viết email, Hermes soát tone, CTA, lỗi và đề xuất bản tốt hơn.',rec:'Tận dụng Hermes như một editor — rủi ro thấp, giá trị cao.'},{v:'3',name:'Tự gửi email',badge:'⚠ CHỈ KHI RULE RẤT RÕ',kind:'danger',desc:'Hermes tự gửi email. Sai một email có thể mất cơ hội bán hàng hoặc gây hiểu lầm.',rec:'Chỉ bật khi quy trình đã ổn và có rule/approval thật chặt.'}]
export function GmailLevels04(){const[cur,setCur]=useState(0);const g=levels[cur];return <div className={styles.glevels04}><div className={styles.glRail04}>{levels.map((x,i)=><button type="button" key={x.v} className={`${styles.glStep04} ${i===cur?styles.glStepOn04:''}`} onClick={()=>setCur(i)}><span>{x.v}</span>{x.name}</button>)}</div><div className={styles.glDetail04}><em className={styles[`gl_${g.kind}`]}>{g.badge}</em><h4>Mức {g.v} · {g.name}</h4><p>{g.desc}</p><div><b>Khuyến nghị:</b> {g.rec}</div></div></div>}
