'use client'
import React,{useState}from'react'
import styles from '../handbook.module.css'
const risks=[{n:'Thấp',s:'Tự làm và báo lại',ex:['Tóm tắt file','Tạo outline','Research công khai'],perm:'Quyền: Hermes tự làm và báo lại kết quả.'},{n:'Trung bình',s:'Làm nháp, chờ duyệt',ex:['Draft email','Tạo báo giá nháp','Đề xuất cập nhật sheet'],perm:'Quyền: Hermes làm bản nháp, chờ CEO duyệt.'},{n:'Cao',s:'Hỏi trước',ex:['Gửi email','Sửa dữ liệu quan trọng','Phát sinh chi phí'],perm:'Quyền: Hermes phải hỏi trước khi làm.'},{n:'Rất cao',s:'Không tự làm',ex:['Xóa dữ liệu','Gửi thông tin nhạy cảm','Thay đổi production'],perm:'Quyền: Không tự làm nếu chưa có lệnh rõ.'}]
export function RiskFlow04(){const[cur,setCur]=useState(0);const r=risks[cur];return <div className={styles.risk04}><div className={styles.riskRail04}>{risks.map((x,i)=><button type="button" key={x.n} className={`${styles.riskStep04} ${cur===i?styles.riskOn04:''}`} onClick={()=>setCur(i)}><i className={styles[`riskDot${i}`]}/><span><b>{x.n}</b><small>{x.s}</small></span></button>)}</div><div className={styles.riskDetail04}><div>Mức rủi ro {cur+1} / 4</div><h4>{r.n}</h4><p>{r.ex.map(x=><span key={x}>{x}</span>)}</p><em>{r.perm}</em></div></div>}
